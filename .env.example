# PostgreSQL (desenvolvimento local via docker-compose)
DATABASE_URL="postgresql://transportabr:transportabr@localhost:5432/transportabr?schema=public"

# Porta do servidor NestJS
PORT=3000

# JWT
JWT_SECRET="troque-este-segredo"
JWT_EXPIRES_IN="7d"

# Login social (opcional até possuir as credenciais)
GOOGLE_CLIENT_ID=""
APPLE_CLIENT_ID=""
